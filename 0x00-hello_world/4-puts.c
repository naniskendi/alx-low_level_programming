#include <stdio.h>
/**
 * main -entry point
 * programming the first function
 * @void: no argument
 * return : 0 to exit
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
